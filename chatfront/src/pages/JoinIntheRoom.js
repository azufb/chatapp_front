import React, { useState } from "react";
import { useHistory } from "react-router-dom";
import axios from "axios";

const JoinIntheRoom = () => {
    // ルーム一覧から参加するルーム名を選択し、クリックすると、入れる。
}

export default JoinIntheRoom;